// ==========================================
// BIBLIOTEKS-DASHBOARD
// ==========================================

// Testdata används eftersom Bibstat API blockerar
// direkta anrop från GitHub Pages genom CORS.
// API-anropet finns dokumenterat separat i api/api.txt.

const libraryData = {
    visits: "2 647 997",
    loans: "917 517"
};


// ==========================================
// VISA SIFFROR I KORTEN
// ==========================================

const visitsElement = document.getElementById("visits");
const loansElement = document.getElementById("loans");

if (visitsElement) {
    visitsElement.textContent = libraryData.visits;
}

if (loansElement) {
    loansElement.textContent = libraryData.loans;
}


// ==========================================
// SKAPA DIAGRAMMET
// ==========================================

const chartElement = document.getElementById("libraryChart");

if (chartElement && typeof Chart !== "undefined") {

    new Chart(chartElement, {

        type: "bar",

        data: {
            labels: [
                "Besök",
                "Utlån"
            ],

            datasets: [{
                label: "Antal",
                data: [
                    2647997,
                    917517
                ],
                backgroundColor: "#E9A9BB",
                borderColor: "#3E2B24",
                borderWidth: 1,
                borderRadius: 8
            }]
        },

        options: {
            responsive: true,

            plugins: {
                legend: {
                    display: false
                }
            },

            scales: {
                y: {
                    beginAtZero: true,
                    title: {
                        display: true,
                        text: "Antal"
                    }
                },

                x: {
                    grid: {
                        display: false
                    }
                }
            }
        }

    });

}